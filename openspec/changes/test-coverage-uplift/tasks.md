# Tasks — test-coverage-uplift

- [ ] `go test ./... -cover`; note 0% packages.
- [ ] Add unit tests for the core logic in each `cmd/*` / internal package.
- [ ] Keep `go test ./... -race` green; record the coverage delta.
